        <ul class="nav navbar-nav navbar-right">
          <% unless @current_role_user.nil? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= @current_role_user.humanize() %><span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">              
                <li>
                  <%= link_to(@current_role_user.humanize(), 
                    {:controller => :nobody, :action => :start_page,
                      :current_role_id => @current_role_user.id}) %>
                </li>
                <% unless @current_user_object.nil? %>
                  <% @current_user_object.role_users.each do |ru| %>
                    <% next if ru == @current_role_user %>
                    <li>
                      <%= link_to(ru.humanize(), 
                        {:controller => :nobody, :action => :start_page,
                          :current_role_id => ru.id}) %>
                    </li>  
                  <% end %>
                <% end %>
              </ul>
            </li>
          <% end %>  
          <li><a><%= @current_user %></a></li>
          <li><a href = "#"><i class = "fa fa-sign-out"></i></a></li>
        </ul>
